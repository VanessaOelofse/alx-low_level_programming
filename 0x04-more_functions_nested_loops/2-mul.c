#include "main.h"
#include <stdio.h>

/**
 * mul - multiplies integers
 * @a: int to be multiplied
 * @b: int to be multiplied
 * Return: the result
 */
int mul(int a, int b)
{
int c;
c = a * b;
return (c);
}

#include "main.h"
#include <stdio.h>

/**
 * print_sign - Prints the sign of a number
 * @n if it is zero
 * Return: 0
 */

int print_sign(int n)
{
if (n > 0)
{
return (1);
}
else if (n < 0)
{
return (-1);
}
else
{
return (0);
}
}
